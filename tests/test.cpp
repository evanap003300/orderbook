/* 
    * This file contains unit tests for the order book matching engine.
    * It uses the Googletest testing framework to define and run test cases.
    * The tests cover various scenarios such as order matching, partial fills,
    * and edge cases to ensure the correctness of the matching engine implementation.
*/
#include <gtest/gtest.h>
#include "itch.hpp"
#include "matching_engine.hpp"
#include "orderbook.hpp"

TEST(MatchingEngineTest, BasicMatching) {
    
}

TEST(MatchingEngineTest, partialFill) {
    
}

TEST(MatchingEngineTest, stockLeftover) {
    
}